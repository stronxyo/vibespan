# Windows scripts must keep CRLF, including inside the ZIP GitHub generates from
# "Download code": cmd.exe mishandles LF-only batch files.
*.bat   text eol=crlf
*.ps1   text eol=crlf
*.cs    text eol=crlf
*.md    text
*.manifest text
